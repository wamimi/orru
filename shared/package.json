{
  "name": "@orru/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Single source of truth for the commitment encoding. Imported by the frontend, the worker and the proving pipeline.",
  "type": "module",
  "main": "commitment.ts",
  "scripts": {
    "test": "tsx commitment.test.ts"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
